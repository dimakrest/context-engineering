# Continuation: a rejected handoff is re-dispatched with the rejection, up to repair_rounds. A
# driver that stops instead is a driver that needs a human for what it can do itself.
category=continuation
file=driver/missions/loop.py
breaks=malformed-handoff-redispatched
control=none
why-no-control=the mutated lines are the re-dispatch branch itself, reached only on a rejected handoff; every trace that reaches it is a second breaks, and one that does not never executes the line (verified: a raise planted there leaves two-feature-happy-path green)
